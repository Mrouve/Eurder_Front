import { ItemOverviewFilterPipe } from './item-overview-filter.pipe';

describe('ItemOverviewFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemOverviewFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
